/**
 * Utility to automatically determine and add trading session tags to a trade's tags.
 * Disabled: returns user tags as-is without automatic session tagging.
 */

export function addSessionTags(tagsArr, entryTimeStr) {
  return tagsArr || [];
}
